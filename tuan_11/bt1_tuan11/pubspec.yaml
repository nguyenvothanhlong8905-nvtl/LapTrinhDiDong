name: bt1_tuan11
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.10.1    # Core của Firebase
  firebase_auth: ^5.4.1     # Dùng cho Authentication
  google_sign_in: ^6.2.2    # Dùng cho đăng nhập Google

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
