name: bt2_tuan11
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.10.1    
  firebase_storage: ^12.4.10 
  file_picker: ^8.0.0   # Dùng để chọn file từ điện thoại

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
